/** The 2:45 overtime line — the single presentation-layer copy of the
 *  threshold lib/analytics cuts on (165s). Timed pulse, marking table,
 *  and the analytics scatter zone all read this. */
export const OVERTIME_SECONDS = 165;
