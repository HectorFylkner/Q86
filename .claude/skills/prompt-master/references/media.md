Verified: 2026-08-12 - register only; by design names no vendor controls.

# Image and video route

Use when the exact generator lacks a verified vendor route. Do not invent model names, switches, aspect-ratio syntax, seed behavior, or negative-prompt support.

Build the prompt from the user's supplied facts:

1. subject and defining attributes;
2. action or temporal change;
3. environment and era;
4. composition, framing, camera movement, and aspect ratio in natural language;
5. lighting, palette, material, and atmosphere;
6. visual medium or production style;
7. required and excluded content;
8. continuity constraints for video.

Resolve contradictions explicitly. Prefer concrete visible properties over mood adjectives. For video, describe one coherent shot or an ordered shot list, including start state, motion, end state, and audio only when supported or requested.

Footer setting: `vendor default; exact controls unverified`.
